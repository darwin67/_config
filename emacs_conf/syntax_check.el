
;; ===================================================
;;   Syntax checking
;; ===================================================

;; Fly-check
;;  need to install texinfo in OS X
;;  - brew install texinfo
;;  - brew link texinfo -f
(el-get-bundle flycheck
  (global-flycheck-mode))
