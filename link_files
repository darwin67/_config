#!/bin/bash

files=( .zshrc .zlogin .sqliterc .ctags .railsrc .rspec .spacemacs .irbrc .pryrc .gemrc .rubocop.yml .gitconfig .gitignore_global .tmux.conf )

for file in ${files[@]}; do
    ln -sf $PWD/$file $HOME/$file
done

